import{describe,it,expect}from'vitest';import{bridgeStateKey,logLevelKey}from'./workspacePresentation';describe('workspace presentation',()=>{it('maps bridge states safely',()=>{expect(['Connected','Connecting','Unavailable'].map(bridgeStateKey)).toEqual(['bridgeState.connected','bridgeState.connecting','bridgeState.unavailable']);expect(bridgeStateKey('Future')).toBeNull()});it('maps log levels',()=>expect(['Information','Warning','Error'].map(x=>logLevelKey(x as never))).toEqual(['logs.severity.information','logs.severity.warning','logs.severity.error']))})
