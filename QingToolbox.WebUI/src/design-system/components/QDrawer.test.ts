import{describe,expect,it}from'vitest';import{mount}from'@vue/test-utils';import QDrawer from'./QDrawer.vue';
describe('QDrawer',()=>{it('opens closes with Escape and exposes a close button',async()=>{const wrapper=mount(QDrawer,{attachTo:document.body,props:{open:true,title:'Details'},slots:{default:'Body'}});expect(document.body.textContent).toContain('Body');await document.querySelector<HTMLElement>('.q-drawer')!.dispatchEvent(new KeyboardEvent('keydown',{key:'Escape',bubbles:true}));expect(wrapper.emitted('close')).toBeTruthy();await document.querySelector<HTMLButtonElement>('.q-drawer .q-icon-button')!.click();expect(wrapper.emitted('close')).toHaveLength(2);wrapper.unmount()})});
