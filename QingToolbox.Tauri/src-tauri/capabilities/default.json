{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the QingToolbox migration shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
