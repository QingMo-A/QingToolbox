# 安全策略

## 支持范围

QingToolbox 目前处于 Alpha 阶段，仅对**最新发布版本**提供安全修复。历史版本不单独维护。

| 发行线 | 状态 |
| --- | --- |
| Tauri 宿主（主线，最新发布 `v0.3.0-alpha`） | 接受安全报告 |
| WPF 宿主（`0.2.x` 线） | 已冻结，仅受理直接影响用户数据安全的报告 |

## 报告漏洞

**请优先使用 GitHub 的私有漏洞报告功能：**

仓库页面 → **Security** → **Report a vulnerability**

这会在非公开渠道中创建一条安全通告，便于在修复发布前保持细节私密。

如果私有报告入口不可用，可以开一个 Issue，但**请勿在其中公开可直接利用的细节**（例如具体的路径穿越载荷、绕过校验的构造方式）。先说明受影响的组件与影响范围，我们会私下联系你获取复现步骤。

报告时请尽量包含：

- 受影响的版本与组件（宿主 / 某个模块 / 安装器 / 打包脚本）。
- 环境信息（Windows 版本、Tauri 还是 WPF 发行线）。
- 可复现的步骤或最小复现包。
- 影响评估（能读取什么、能写入什么、是否需要用户交互）。

## 安全模型

理解以下边界有助于判断一个问题是否属于安全漏洞。

**宿主提供的保证**

- 模块在**独立操作系统进程**中运行，宿主不建立通用文件系统、进程或窗口桥。
- 模块只能调用自己清单 `operations` / `events` 白名单中声明的操作；不声明即无调用面。
- 模块 Web 界面通过受控 `qmod://` 协议加载，页面无法指定任意模块 ID 或提交可执行路径。
- `.qmod` 导入执行 ZIP 结构与路径安全校验（拒绝绝对路径、路径穿越、重复项、符号链接、加密条目，并限制条目数、单文件大小、总展开大小与压缩比），通过校验后以同卷原子重命名发布，且不覆盖已存在的同 ID 目录。
- 官方模块更新与宿主自更新均校验版本、包大小与 SHA256。
- 单实例激活管道对输入执行严格长度限制。
- 模块窗口的 `module-*` capability 只授予 Tauri core IPC，不继承文件系统或进程权限。

**明确不构成安全边界**

- **`permissions` 字段只是声明**，用于向用户告知模块意图，不是强制沙箱。
- **`.qmod` 尚不支持包签名。** 校验能保证包的完整性与结构安全，但无法证明来源可信。
- **模块加载后拥有当前用户权限**，其行为不受宿主强制约束。因此只应导入可信来源的模块。
- 宿主二进制与安装器**未代码签名**，无法防御下载渠道被篡改；请校验 Release 提供的同名 `.sha256`。

## 已知的供应链前提

- 正式分发只提供 Inno Setup 安装器及其同名 `.sha256`。请只从本仓库的 GitHub Release 下载。
- 安装器按用户安装，不需要管理员权限，也不会触发 UAC 提权。
- 自动更新只从官方 GitHub Release 拉取，并校验资产身份、大小与 SHA256；应用不会自行替换程序文件，最终由 Inno Setup 完成原地覆盖。

## 不在范围内的报告

- 需要攻击者已具备本机管理员权限或已能修改用户文件的问题。
- 未签名的既有事实（这是已知的 Alpha 限制，见上文）。
- 依赖第三方运行时（Everything、qpdf 等）上游自身的安全问题；请同时向其上游报告。

## 相关文档

- 安全暂存边界：[`docs/QMOD_STAGING_SECURITY.md`](docs/QMOD_STAGING_SECURITY.md)
- 模块包格式与校验规则：[`docs/QMOD_FORMAT.md`](docs/QMOD_FORMAT.md)
- 模块更新事务边界：[`docs/MODULE_UPDATE_TRANSACTION.md`](docs/MODULE_UPDATE_TRANSACTION.md)
- 宿主/模块通信契约：[`protocol/README.md`](protocol/README.md)
